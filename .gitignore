# Local env (use Cursor Cloud Agent secrets for sensitive values)
.env
.env.*
!.env.example

# Optional override for `config/git-path.placeholder` (copy or replace)
config/git-path

# OS / editor
.DS_Store
*.swp
*~
