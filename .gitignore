# Local env (use Cursor Cloud Agent secrets for sensitive values)
.env
.env.*
!.env.example

# Optional override for `git/git-path.placeholder` (copy or replace)
git/git-path

# OS / editor
.DS_Store
*.swp
*~
