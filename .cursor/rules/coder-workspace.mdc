---
description: Coder workspace context — where the agent runs and how to report it
alwaysApply: true
---

# Coder workspace

This repository is meant to be opened as a **Coder** remote workspace (DataKnife templates such as DKAI DevPod / Arch / Agent). The Cursor agent often runs **inside** that Coder-provisioned environment, not only on a local machine. Treat paths, `coder` CLI, and cluster/network assumptions accordingly.

## Location note at conversation start

At the **start of a new agent conversation** (first assistant turn after the user opens or switches to this workspace), establish **where** the agent is running before doing substantive work:

1. Run a short shell snippet that prints **Coder-related environment** without leaking secrets. Prefer listing names and safe values; **never** print `CODER_AGENT_TOKEN` or other token values in full (show `CODER_AGENT_TOKEN=<set>` or omit the line).
2. Optionally add `pwd`, `hostname`, and `coder version` / `coder whoami` if `coder` is on `PATH` and non-interactive auth is available.

Example (adapt if `env` output is empty—Coder may expose few `CODER_*` vars depending on template):

```bash
env | grep '^CODER_' | grep -v '^CODER_AGENT_TOKEN=' || true
echo "PWD=$(pwd) HOST=$(hostname)"
command -v coder >/dev/null && coder version 2>/dev/null | head -1
```

3. Reply with a **brief note** (a few lines) summarizing: Coder workspace context, working directory, host, and any `CODER_*` names present (values redacted where sensitive).

If the workspace is clearly local-only and no `CODER_*` vars exist, say so once instead of repeating every message.

## Agent repos and multiple projects (not Git worktrees)

On **Coder** workspaces, this template is expected to live at **`/home/coder/agent-workspace`** (i.e. `~/agent-workspace` for the `coder` user).

Put **each additional repository or application project**—anything that is not this template—under:

**`/home/coder/agent-workspace/git/<project>/`**

Use **one directory per repo** (separate clones: `git/api/`, `git/web/`, …) so you can run **multiple Cursor sessions or a multi-root workspace** (`.code-workspace`) across several projects without stuffing unrelated code into the template root. This layout is for **working across multiple repos/projects**, not for describing Git’s `git worktree` command.

**Cursor worktrees** (chat commands like `/worktree` and `/best-of-n`, CLI `--worktree`) are Cursor’s **isolated checkouts** for agent work; they are managed under **`~/.cursor/worktrees`** by default and are documented separately—see [Cursor Worktrees](https://cursor.com/docs/configuration/worktrees). Do not confuse that mechanism with the `git/` sibling-repo layout above.

Keep **`/home/coder/agent-workspace`** itself for this template only: skills (`.cursor/skills/`), hooks, and shared rules.

**Locally** (not on Coder), mirror the same layout: `<clone-of-this-repo>/git/<project>/`.

## References

- Coder CLI skill: `.cursor/skills/agent-skills/skills/coder-cli/SKILL.md`
- Coder docs: [Workspaces](https://coder.com/docs/workspaces)
